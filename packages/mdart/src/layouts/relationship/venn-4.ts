// Legacy alias: the unified `venn` renderer auto-selects the 4-circle layout
// when there are 4 non-intersection items. Kept as a separate type so existing
// `type: venn-4` diagrams keep working unchanged.
export { render } from './venn'
