// Legacy alias: the unified `venn` renderer auto-selects the 3-circle layout
// when there are 3 non-intersection items. Kept as a separate type so existing
// `type: venn-3` diagrams keep working unchanged.
export { render } from './venn'
