# Allowlist approach — exclude everything, then explicitly re-include what the VSIX needs.
# This prevents monorepo parent directories and workspace deps from leaking in.
**

!dist/**
!icon.png
!package.json
!README.md
!LICENSE
!CHANGELOG.md
